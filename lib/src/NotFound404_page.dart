import 'package:flutter/material.dart';

class NotFound404Page extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('No encontrada'),
      ),
      body: Text('Not Found 404'),
    );
  }
}